apiVersion: v1
kind: Service
metadata:
  name: {{ include "copy-service.fullname" . }}
  labels:
    {{- include "copy-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "copy-service.selectorLabels" . | nindent 4 }}
