kind: GitRepo
apiVersion: fleet.cattle.io/v1alpha1
metadata:
  name: {{ .Values.name }}
  namespace: fleet-local
spec:
  repo: {{ .Values.repo }}
  branch: {{ .Values.branch }}
  paths:
  - system/components/*
