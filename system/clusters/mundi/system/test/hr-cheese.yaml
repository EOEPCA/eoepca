apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: cheese
  namespace: test
spec:
  chart:
    spec:
      chart: ./system/charts/cheese
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
  values:
    baseHostname: 80.158.50.102.nip.io
  interval: 1m0s
