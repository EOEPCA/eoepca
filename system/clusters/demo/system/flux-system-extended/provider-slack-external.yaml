apiVersion: notification.toolkit.fluxcd.io/v1beta1
kind: Provider
metadata:
  name: slack-external
  namespace: flux-system
spec:
  type: slack
  channel: eoepca-operations-external
  secretRef:
    name: slack-url-external