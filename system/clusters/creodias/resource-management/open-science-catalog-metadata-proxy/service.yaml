apiVersion: v1
kind: Service
metadata:
  name: app-reverse-proxy
  namespace: osc
spec:
  ports:
  - name: http
    protocol: TCP
    targetPort: 80
    port: 80
  selector:
    app: app-reverse-proxy
    category: reverse-proxy
