---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ workspace_name }}-pep-pvc"
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: managed-nfs-storage
  resources:
    requests:
      storage: 5Gi
