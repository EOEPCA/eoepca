apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: um-identity-service
  namespace: um
  annotations:
    meta.helm.sh/release-name: um-identity-service
    meta.helm.sh/release-namespace: um
spec:
  chart:
    spec:
      chart: identity-service
      version: 1.0.85
      sourceRef:
        kind: HelmRepository
        name: eoepca
        namespace: common
  values:
    volumeClaim:
      name: eoepca-userman-pvc
      create: false
    identity-keycloak:
      ingress:
        enabled: true
        className: "nginx"
        annotations:
          cert-manager.io/cluster-issuer: letsencrypt
        hosts:
          - host: identity.keycloak.demo.eoepca.org
            paths:
              - path: /
                pathType: Prefix
        tls:
          - secretName: identity-keycloak-tls-certificate
            hosts:
              - identity.keycloak.demo.eoepca.org
    identity-postgres:
      volumeClaim:
        name: eoepca-userman-pvc
    identity-api:
      ingress:
        enabled: true
        className: "nginx"
        annotations:
          cert-manager.io/cluster-issuer: letsencrypt
        hosts:
          - host: identity.api.demo.eoepca.org
            paths:
              - path: /
                pathType: Prefix
        tls:
          - secretName: identity-api-tls-certificate
            hosts:
              - identity.api.demo.eoepca.org
      configMap:
        authServerUrl: https://identity.keycloak.demo.eoepca.org
    identity-api-gatekeeper:
      nameOverride: identity-api-protection
      config:
        client-id: identity-api
        discovery-url: https://identity.keycloak.demo.eoepca.org/realms/master
        cookie-domain: demo.eoepca.org
      targetService:
        host: identity-api-protected.demo.eoepca.org
        name: identity-api
        port:
          number: 8080
      ingress:
        enabled: true
        className: "nginx"
        annotations:
          cert-manager.io/cluster-issuer: letsencrypt
  timeout: 5m0s
  interval: 1m0s