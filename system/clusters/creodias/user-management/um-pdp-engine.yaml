apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: um-pdp-engine
  namespace: default
spec:
  chart:
    spec:
      chart: pdp-engine
      version: 1.0.0
      sourceRef:
        kind: HelmRepository
        name: eoepca
        namespace: common
  values:
    imagePullSecrets:
      - name: regcred
    global:
      nginxIp: 80.158.54.66
      domain: test.mundi.eoepca.org
    volumeClaim:
      name: eoepca-userman-pvc
      create: false

  timeout: 25m0s
  interval: 1m0s
