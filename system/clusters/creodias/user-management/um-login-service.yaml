apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: um-login-service
  namespace: default
spec:
  chart:
    spec:
      chart: login-service
      version: 0.9.45
      sourceRef:
        kind: HelmRepository
        name: eoepca
        namespace: common
  values:
    volumeClaim:
      name: eoepca-userman-pvc
      create: false
    config:
      domain: auth.mundi.eoepca.org
      volumeClaim:
        name: managed-nfs-storage
    opendj:
      # This can be useful to workaround helm 'failed to upgrade' errors due to
      # immutable fields in the 'um-login-service-persistence-init-ss' job
      # persistence:
      #   enabled: false
      volumeClaim:
        name: managed-nfs-storage
    oxauth:
      #image: 
      #  tag: 4.2.3_08
      volumeClaim:
        name: managed-nfs-storage
    oxtrust:
      #image: 
      #  tag: 4.2.3_04
      volumeClaim:
        name: managed-nfs-storage
    global:
      domain: auth.mundi.eoepca.org
      nginxIp: 80.158.54.66
    nginx:
      ingress:
        annotations:
          #kubernetes.io/elb.ip: 80.158.51.219
          cert-manager.io/cluster-issuer: letsencrypt
        hosts:
          - auth.mundi.eoepca.org
        tls:
          - hosts:
              - auth.mundi.eoepca.org
            secretName: login-service-tls
  timeout: 25m0s
  interval: 1m0s
