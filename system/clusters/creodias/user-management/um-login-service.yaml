apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: um-login-service
  namespace: default
spec:
  chart:
    spec:
      chart: login-service
      sourceRef:
        kind: HelmRepository
        name: eoepca
        namespace: common
  values:
    config:
      domain: test.80.158.51.219.nip.io
      volumeClaim:
        name: managed-nas-storage
    opendj:
      volumeClaim:
        name: managed-nas-storage
    oxauth:
      #image: 
      #  tag: 4.2.3_08
      volumeClaim:
        name: managed-nas-storage
    oxtrust:
      #image: 
      #  tag: 4.2.3_04
      volumeClaim:
        name: managed-nas-storage
    global:
      domain: test.80.158.51.219.nip.io
      nginxIp: 80.158.51.219
    nginx:
      ingress:
        annotations:
          #kubernetes.io/elb.ip: 80.158.51.219
          cert-manager.io/cluster-issuer: letsencrypt
        hosts:
          - test.80.158.51.219.nip.io
        tls:
          - hosts:
              - test.80.158.51.219.nip.io
            secretName: login-service-tls
  timeout: 25m0s
  interval: 1m0s
