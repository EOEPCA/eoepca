apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: storage
  namespace: default
spec:
  chart:
    spec:
      chart: storage
      sourceRef:
        kind: HelmRepository
        name: eoepca
        namespace: common
  values:
    host:
      enabled: true
    nfs:
      enabled: false
  interval: 1m0s
