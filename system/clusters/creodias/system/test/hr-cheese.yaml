apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: cheese
  namespace: test
spec:
  chart:
    spec:
      chart: cheese
      version: 0.9.3
      sourceRef:
        kind: HelmRepository
        name: eoepca
        namespace: common
  values:
    baseHostname: mundi.eoepca.org
    pullSecret: regcred
  interval: 1m0s
