apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: dummy-service-pep
  namespace: test
spec:
  chart:
    spec:
      chart: pep-engine
      sourceRef:
        kind: HelmRepository
        name: eoepca
        namespace: common
  values:
    context: dummy-service
    global:
      pep: dummy-service-pep
      domain: test.80.158.54.66.nip.io
      nginxIp: 80.158.54.66
    configMap:
      resourceServer: http://dummy-service/
    image:
      pullPolicy: Always
    certManager:
      clusterIssuer: letsencrypt
    volumeClaim:
      name: dummy-service-pep-pvc
      create: false

  timeout: 25m0s
  interval: 1m0s
