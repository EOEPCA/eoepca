apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: dummy-service
  namespace: test
spec:
  chart:
    spec:
      chart: dummy
      version: 0.9.2
      sourceRef:
        kind: HelmRepository
        name: eoepca
        namespace: common
  values:
    ingress:
      enabled: true
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt
        kubernetes.io/ingress.class: nginx
      hosts:
        - host: dummy-service-open.develop.eoepca.org
          paths:
            - path: /
      tls:
       - secretName: dummy-service-open-tls
         hosts:
           - dummy-service-open.develop.eoepca.org
  interval: 1m0s
