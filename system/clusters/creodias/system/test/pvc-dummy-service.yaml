apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dummy-service-pep-pvc
  namespace: test
spec:
  storageClassName: csi-nas
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 3Gi
