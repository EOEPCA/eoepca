apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: infra-pvc
  namespace: infra
spec:
  storageClassName: managed-nfs-storage
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 3Gi
