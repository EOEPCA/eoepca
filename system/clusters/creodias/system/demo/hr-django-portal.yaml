---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: django-portal
  namespace: demo
spec:
  chart:
    spec:
      chart: django-portal
      version: 0.1.10
      sourceRef:
        kind: HelmRepository
        name: eoepca
        namespace: common
  values:
    domain: demo.eoepca.org
    configmap:
      user_prefix: rm-user
  interval: 1m0s
