apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt
spec:
  acme:
    email: sa@eox.at
    server: https://acme-staging-v02.api.letsencrypt.org/directory
    privateKeySecretRef:
      name: letsencrypt
    solvers:
    - http01:
        ingress:
          class: nginx
        # ingress:
        #   ingressTemplate:
        #     metadata:
        #       annotations:
        #         kubernetes.io/elb.ip: 80.158.54.66