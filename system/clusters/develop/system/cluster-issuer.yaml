apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-tmp
spec:
  acme:
    email: sa@eox.at
    server: https://acme-v02.api.letsencrypt.org/directory
    privateKeySecretRef:
      name: letsencrypt-tmp
    solvers:
    - http01:
        ingress:
          class: nginx